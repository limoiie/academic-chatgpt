export { };

declare global {
  interface CreateEmbeddingsConfigFormState {
    client: string;
    configName: string;
    meta: any;
  }

  interface CreateIndexConfigFormState {
    client: string;
    configName: string;
    meta: any;
  }

}
